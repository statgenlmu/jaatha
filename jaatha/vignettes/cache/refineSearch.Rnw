<<setupRefSearch, include=FALSE, echo=FALSE>>=
library(jaatha)
load('saved_objects/picked-points.Rsave')
load('saved_objects/jaatha.Rsave')
@

<<RefineSearch>>=
jaatha <- Jaatha.refineSearch(jaatha, picked.points, nSim=100)
@

<<SaveRefSearchResults, include=FALSE, echo=FALSE>>=
save(jaatha, file='saved_objects/jaatha-complete.Rsave')
@
